# 🖼️ IMAGES

- `us-together.png` → the reunion photo shown after the runner game + on the HAPPY 20 finale.
  Recommended: square-ish JPG/PNG, ~800x800. Just name it exactly `us-together.png`.
- Missing? A cute gradient placeholder card is shown automatically — the game never crashes.

# 🎵 MUSIC (all optional)

Drop mp3s with these EXACT names:
- `happy-birthday.mp3` → cake + 20-wishes + finale (loops)
- `memories.mp3` → memories section (loops)
- `game-loop.mp3` → runner game (loops)

If a file is missing, the built-in chiptune synth plays instead. No errors.
